package com.example.core.gui.iface.base;

/**
 * Represents a widget on GUI. Usually widget is a common complex element, which is no frame and no modal dialog. For
 * example, widgets are tables, field sets, tabs and so on.
 */
public interface Widget extends ComplexGUIElement {
}
